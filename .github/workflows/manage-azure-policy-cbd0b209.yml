name: manage-azure-policy-cbd0b209
on:
  workflow_dispatch:
    inputs:
      remarks:
        description: 'Reason for triggering the workflow run'
        required: false
        default: 'Updating Azure Policies'


## Uncomment the following if you wish to automatically trigger workflow run with every commit
#
#  push:
#    paths:
#      - 'policies/IoT_Hub_device_provisioning_service_instances_should_disable_public_network_access_d82101f3-f3ce-4fc5-8708-4c09f4009546/**'
#      - .github/workflows/manage-azure-policy-cbd0b209.yml
jobs:
  apply-azure-policy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZURE_CREDENTIALS_cbd0b209}}
        allow-no-subscriptions: true
    - name: Create or Update Azure Policies
      uses: azure/manage-azure-policy@v0
      with:
        paths: |
          policies/IoT_Hub_device_provisioning_service_instances_should_disable_public_network_access_d82101f3-f3ce-4fc5-8708-4c09f4009546/**
