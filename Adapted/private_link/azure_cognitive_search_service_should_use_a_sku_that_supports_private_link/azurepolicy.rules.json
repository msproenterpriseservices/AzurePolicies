{
    "if":  {
               "allOf":  [
                             "@{field=type; equals=Microsoft.Search/searchServices}",
                             "@{field=Microsoft.Search/searchServices/sku.name; equals=free}"
                         ]
           },
    "then":  {
                 "effect":  "[parameters(\u0027effect\u0027)]"
             }
}
